services:
  mariadb:
    image: mariadb:11.8
    container_name: beercounter-mariadb-prod
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
    ports:
      - "${MARIADB_PORT:-3307}:3306"
    volumes:
      - mariadb_data_prod:/var/lib/mysql
    networks:
      - beercounter-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 30s
      timeout: 10s
      retries: 3

  backend:
    image: beercounter-backend:latest
    container_name: beercounter-backend-prod
    environment:
      DB_HOST: mariadb
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_PORT: 3306
      PORT: 3001
    ports:
      - "${BACKEND_PORT}:3001"
    depends_on:
      mariadb:
        condition: service_healthy
    networks:
      - beercounter-network
    restart: unless-stopped

  frontend:
    image: beercounter-frontend:latest
    container_name: beercounter-frontend-prod
    ports:
      - "${FRONTEND_PORT}:80"
    depends_on:
      - backend
    networks:
      - beercounter-network
    restart: unless-stopped

networks:
  beercounter-network:
    driver: bridge

volumes:
  mariadb_data_prod:
